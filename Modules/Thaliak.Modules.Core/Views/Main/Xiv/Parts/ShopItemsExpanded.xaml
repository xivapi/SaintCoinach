<UserControl x:Class="Thaliak.Modules.Core.Views.Main.Xiv.Parts.ShopItemsExpanded"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:data="clr-namespace:Thaliak.Modules.Core.Data"
             xmlns:xiv="clr-namespace:SaintCoinach.Xiv;assembly=SaintCoinach"
             xmlns:conv="clr-namespace:Thaliak.Modules.Core.Converters"
             xmlns:bConv="http://schemas.kent.boogaart.com/converters"
             xmlns:listItems="clr-namespace:Thaliak.Modules.Core.Views.ListItems"
             xmlns:Thaliak="clr-namespace:Thaliak;assembly=Thaliak.Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="ShopItemTemplate" DataType="{x:Type xiv:IShopItem}">
            <Border BorderBrush="{DynamicResource SeperatorBrush}" BorderThickness="1"
                    CornerRadius="4"
                    Padding="2"
                    Margin="0 2">
                <listItems:ShopItemExpanded NavigateToCostItems="True"
                                            NavigateToENpcs="True"
                                            NavigateToShopItem="True"
                                            NavigateToShops="True" />
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Expander ExpandDirection="Down"
              x:Name="_Expander">
        <ItemsControl ItemsSource="{Binding}" Margin="2"
                 HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                 ItemTemplate="{StaticResource ShopItemTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel IsItemsHost="True" Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Expander>
</UserControl>
